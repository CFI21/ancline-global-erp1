name: ANCLINE Legacy Web Promotion Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Enforce post-acceptance release path
        shell: bash
        run: |
          echo "::error::Legacy direct Web promotion is disabled in POST-ACCEPTANCE MODE."
          echo "::error::Create an APPROVED infra/change-control/active-change-request.json and use ANCLINE Smart Release Hardened."
          exit 1
